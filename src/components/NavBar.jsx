const NavBar = () => {
  return (
    <header className="navbar">
      <div className="inner">
        <a className="logo" href="#hero">
          Trey
        </a>
      </div>
    </header>
  );
};

export default NavBar;
